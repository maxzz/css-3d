import { atom } from 'jotai';

export const rotAtom = atom({x: 0, y: 0});
export const rotActiveAtom = atom(false);

export const cubeWAtom = atom(0);
export const cubeHAtom = atom(0);
export const cubeDAtom = atom(0);
